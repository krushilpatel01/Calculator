// 1) ways to print in java script
    // - console.log("text");
    // - alert("text");
    // - document.write("text");


// 2) javascrript console Api 
    // - console.log("text");
    // - console.error("text");
    // - console.warn("text");


// 3) javascript variable ( var, let, const)
//     containers to store values;
//     var number1=34;
//     var number2=64;
//     console.log(number1+number2);
// var = variable can be re-declare in program
// let = variable can not redeclare this scope but you can update it
// const = can not redeclare and can not update it

// 4) datTYPES IN Javascript
// - primitive datatype ->number, string, boolean, undefined, null, bigint, symbol.
// - non primitive datatype -> object(array, function)

// 5) datatype in javascript with array demo
    // in javascript two type of datatype 
    // 1)primitive data type = number, string, booleanl, symbol, underfined, null
    // 2)refrence datatype = array and objects

    // number
    var number1 = 34;
    var number2 = 64;

    // string
    var str1 = "this is state";
    var str2 = 'this is state';

    // objects
    var marks = {
        ravi: 34,
        ck: 99.99,
        jay: 38,
        karan: 29,
    }
    console.log(marks);

    // boolean
    var a = true;
    var b = false;
    console.log(a, b);

    // var und = underfined and null
    var und;
    console.log(und);

    var n = null;
    console.log(n);

    // array
    var arr = [1,2,"string",4,5];
    console.log(arr[2]);

    console.clear();

// operators in javascript
    // 1) arithmatic operator
    var z =10;
    var v =20;

    console.log("this is arithmatic operation:",z+v);
    console.log("this is arithmatic operation:",z-v);
    console.log("this is arithmatic operation:",z*v);
    console.log("this is arithmatic operation:",z/v);

    // 2) assignment operator
    var x = v;
    // x +=v;
    // x -=v;
    // x *=v;
    x /=v;
    console.log(x);
    // var x =1;
    var y =30;
    console.log(x == y);
    console.log(x >= y);
    console.log(x <= y);
    console.log(x < y);
    console.log(x > y);
    console.clear();

    // 3) logical operator
    // - logical and
    // console.log(true && true);
    // console.log(true && false);
    // console.log(false && true);
    // console.log(false && false);

     // - logical or
     console.log(true || true);
     console.log(true || false);
     console.log(false|| true);
     console.log(false|| false);

    //  logical not
    console.log(!false);
    console.log(!true);

    console.clear();
    

